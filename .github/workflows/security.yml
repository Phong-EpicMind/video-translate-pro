name: Security Scan

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: "24 3 * * 1"

permissions:
  contents: read

jobs:
  bandit:
    name: Bandit Python security scan
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install Bandit
        run: |
          python -m pip install --upgrade pip
          python -m pip install "bandit[toml]"

      - name: Run Bandit
        run: |
          bandit -r app.py desktop.py utils.py ptts_fallback.py scratch -ll
